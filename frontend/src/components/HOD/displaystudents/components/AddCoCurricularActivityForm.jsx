import React, { useState } from 'react';

const AddCoCurricularActivityForm = ({ activity, onClose, onSubmit }) => {

    return (
        <div className="activity-form-overlay">

        </div>
    );
};

export default AddCoCurricularActivityForm;
